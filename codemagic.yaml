workflows:
  ios-unsigned:
    name: iOS Unsigned IPA
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      node: 20
      xcode: 16.2
      cocoapods: default
      groups:
        - soundscape
    scripts:
      - name: Install dependencies
        script: |
          set -e
          npm install
      - name: Create .env file
        script: |
          set -e
          cat > .env << EOF
          EXPO_PUBLIC_FIREBASE_API_KEY=$EXPO_PUBLIC_FIREBASE_API_KEY
          EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=$EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN
          EXPO_PUBLIC_FIREBASE_PROJECT_ID=$EXPO_PUBLIC_FIREBASE_PROJECT_ID
          EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=$EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET
          EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=$EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
          EXPO_PUBLIC_FIREBASE_APP_ID=$EXPO_PUBLIC_FIREBASE_APP_ID
          EXPO_PUBLIC_FIREBASE_MEASUREMENT_ID=$EXPO_PUBLIC_FIREBASE_MEASUREMENT_ID
          EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=$EXPO_PUBLIC_GOOGLE_MAPS_API_KEY
          EXPO_PUBLIC_AGORA_APP_ID=$EXPO_PUBLIC_AGORA_APP_ID
          EOF
      - name: Expo Prebuild (no pod install)
        script: |
          set -e
          npx expo prebuild --platform ios --clean --non-interactive --no-install
          echo "=== ios/ after prebuild ==="
          ls -la ios/
          echo "=== Podfile exists? ==="
          test -f ios/Podfile && echo "YES - Podfile found" || echo "NO - Podfile MISSING"
      - name: Patch Expo.plist — forza expo-updates channel preview
        script: |
          set -e
          echo "=== Cerco Expo.plist ==="
          find ios -name "Expo.plist" 2>/dev/null || echo "Expo.plist non trovato"
          find ios -name "*.plist" | head -20
          PLIST=$(find ios -name "Expo.plist" | head -1)
          if [ -z "$PLIST" ]; then
            echo "Expo.plist non trovato, skip patch"
            exit 0
          fi
          echo "=== Expo.plist trovato: $PLIST ==="
          echo "=== PRIMA del patch ==="
          cat "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :EXUpdatesURL https://u.expo.dev/1acc4f41-619c-423f-8db0-fcc6e7243ba2" "$PLIST" 2>/dev/null || \
            /usr/libexec/PlistBuddy -c "Add :EXUpdatesURL string https://u.expo.dev/1acc4f41-619c-423f-8db0-fcc6e7243ba2" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :EXUpdatesEnabled true" "$PLIST" 2>/dev/null || \
            /usr/libexec/PlistBuddy -c "Add :EXUpdatesEnabled bool true" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :EXUpdatesRuntimeVersion 1.0.0" "$PLIST" 2>/dev/null || \
            /usr/libexec/PlistBuddy -c "Add :EXUpdatesRuntimeVersion string 1.0.0" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :EXUpdatesChannelName preview" "$PLIST" 2>/dev/null || \
            /usr/libexec/PlistBuddy -c "Add :EXUpdatesChannelName string preview" "$PLIST"
          echo "=== DOPO il patch ==="
          cat "$PLIST"
      - name: CocoaPods install
        script: |
          set -e
          cd ios
          echo "=== Pod version ==="
          pod --version
          echo "=== Running pod install ==="
          pod install --repo-update
          echo "=== ios/ after pod install ==="
          ls -la
          echo "=== xcworkspace check ==="
          test -d soundscapemobile.xcworkspace && echo "xcworkspace CREATED OK" || { echo "xcworkspace MISSING - FAIL"; exit 1; }
      - name: Build unsigned archive
        script: |
          set -e
          WORKSPACE="ios/soundscapemobile.xcworkspace"
          SCHEME="soundscapemobile"
          echo "Using workspace: $WORKSPACE, scheme: $SCHEME"
          xcodebuild \
            -workspace "$WORKSPACE" \
            -scheme "$SCHEME" \
            -configuration Release \
            -archivePath "$CM_BUILD_DIR/soundscape.xcarchive" \
            -destination "generic/platform=iOS" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            archive
      - name: Export IPA
        script: |
          set -e
          mkdir -p "$CM_BUILD_DIR/Payload"
          cp -r "$CM_BUILD_DIR/soundscape.xcarchive/Products/Applications/"*.app "$CM_BUILD_DIR/Payload/"
          cd "$CM_BUILD_DIR" && zip -r soundscape.ipa Payload/
    artifacts:
      - $CM_BUILD_DIR/soundscape.ipa

  android-release:
    name: Android Release APK (unsigned)
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      node: 20
      java: 17
      groups:
        - soundscape
    scripts:
      - name: Install dependencies
        script: |
          set -e
          npm install
      - name: Create .env file
        script: |
          set -e
          cat > .env << EOF
          EXPO_PUBLIC_FIREBASE_API_KEY=$EXPO_PUBLIC_FIREBASE_API_KEY
          EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=$EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN
          EXPO_PUBLIC_FIREBASE_PROJECT_ID=$EXPO_PUBLIC_FIREBASE_PROJECT_ID
          EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=$EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET
          EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=$EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
          EXPO_PUBLIC_FIREBASE_APP_ID=$EXPO_PUBLIC_FIREBASE_APP_ID
          EXPO_PUBLIC_FIREBASE_MEASUREMENT_ID=$EXPO_PUBLIC_FIREBASE_MEASUREMENT_ID
          EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=$EXPO_PUBLIC_GOOGLE_MAPS_API_KEY
          EXPO_PUBLIC_AGORA_APP_ID=$EXPO_PUBLIC_AGORA_APP_ID
          EOF
      - name: Expo Prebuild Android
        script: |
          set -e
          npx expo prebuild --platform android --clean --non-interactive --no-install
      - name: Generate keystore
        script: |
          set -e
          keytool -genkeypair -v \
            -keystore android/app/soundscape.keystore \
            -alias soundscape \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass soundscape123 -keypass soundscape123 \
            -dname "CN=Soundscape, OU=Dev, O=Soundscape, L=IT, S=IT, C=IT"
      - name: Build release APK
        script: |
          set -e
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon \
            -Pandroid.injected.signing.store.file=soundscape.keystore \
            -Pandroid.injected.signing.store.password=soundscape123 \
            -Pandroid.injected.signing.key.alias=soundscape \
            -Pandroid.injected.signing.key.password=soundscape123
    artifacts:
      - android/app/build/outputs/apk/release/app-release.apk
